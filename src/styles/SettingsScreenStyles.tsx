import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  textWrapper: {
    borderColor: 'black',
    borderRadius: 5,
  },
});

export default styles;
