import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  profileImg: {
    width: 100,
    height: 100,
    borderRadius: 50,
  },
});

export default styles;
